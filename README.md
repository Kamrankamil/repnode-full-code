# repnode-full-code

REP node backend, staking service, and React frontend.

Copy `.env.example` to the appropriate local environment files and provide real credentials before running the services. Never commit private keys or passwords.

